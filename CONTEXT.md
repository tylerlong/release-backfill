# Release Backfill

The language used to describe package versions considered for GitHub release backfills.

## Language

**Prerelease**:
A non-final package version in the `alpha`, `beta`, or `rc` channel. Other SemVer prerelease identifiers are outside this tool's release vocabulary.
_Avoid_: Pre-release, preview release
